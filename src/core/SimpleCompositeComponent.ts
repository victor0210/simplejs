import SimpleComponent from '../implements/SimpleComponent'

/**
 * @description
 * return a global static function collection
 * */
export default class SimpleCompositeComponent extends SimpleComponent {
    constructor(spec: any) {
        super(spec);
    }

    mountComponent(): void {
    }

    updateComponent(): void {
    }

    unmountComponent(): void {
    }
}